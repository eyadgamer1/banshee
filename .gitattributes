# Normalize line endings to LF in the repo; check out as-is on Windows
* text=auto eol=lf

# Binaries — never touch
*.png binary
*.jpg binary
*.ico binary
*.db  binary
*.sqlite binary
uv.lock text eol=lf
