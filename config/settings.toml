[llm]
backend = "ollama"
model = "mistral-nemo"
base_url = "http://localhost:11434"
timeout_seconds = 60
max_retries = 3

[scan]
default_timeout = 5.0
stealth_delay_ms = 200
max_concurrent_probes = 50

[store]
db_path = "output/pps.db"

[report]
output_dir = "output"
formats = ["json", "markdown"]
